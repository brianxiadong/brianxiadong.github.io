# Jekyll 最小博客

使用 Jekyll 构建的个人博客。

## 本地运行

1. 安装依赖：
   ```bash
   bundle install
   ```

2. 启动本地服务器：
   ```bash
   bundle exec jekyll serve
   ```

3. 访问 http://localhost:4000

## 添加文章

在 `_posts` 目录下创建 Markdown 文件，文件名格式：`YYYY-MM-DD-title.md`

## 部署

推送到 GitHub，GitHub Pages 会自动构建和部署。
